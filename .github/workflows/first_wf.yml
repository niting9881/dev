name: my_workflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: job1
              run: echo "hello Nitin"

            - name: list files
              run: ls

            - name: read file
              run: cat README.md


